:: DeleteEvidence [nobr] {"position":"375,2850","size":"100,100"}
<<set _randomIndex to Math.floor(Math.random() * $ghost.evidence.length)>>
<<set $hiddenEvidence to $ghost.evidence[_randomIndex]>>
<<set $ghost.evidence.deleteAt(_randomIndex)>>
<<if $deleteSecondEvidence eq 1>>
	<<set _randomIndex1 to Math.floor(Math.random() * $ghost.evidence.length)>>
	<<set $hiddenEvidence1 to $ghost.evidence[_randomIndex1]>>
	<<set $ghost.evidence.deleteAt(_randomIndex1)>>
<</if>>
<<if $deleteThirdEvidence eq 1>>
	<<set _randomIndex2 to Math.floor(Math.random() * $ghost.evidence.length)>>
	<<set $hiddenEvidence2 to $ghost.evidence[_randomIndex2]>>
	<<set $ghost.evidence.deleteAt(_randomIndex2)>>
<</if>>
